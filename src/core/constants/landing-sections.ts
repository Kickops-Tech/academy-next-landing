/**
 * Anchor ids for in-page CTAs (hero → second fold, etc.).
 * Kept in core so features can scroll without importing each other.
 */
export const LANDING_SECTION_ID = {
  target: "pra-quem",
} as const;
