/**
 * Shared mobile display sizes for two-word LP section titles.
 * Sized in `cqw` inside each feature’s `@container` stage so optical size
 * matches across folds when stages are ~full viewport width.
 */
export const SECTION_DISPLAY_HEADING_MOBILE = {
  lead: "text-[7.8cqw]",
  display: "text-[19.5cqw]",
} as const;
